import {} from "@mui/icons-material";
import {} from "@mui/material";

export default function AdminDishTable() {
    return (
        <>
        {/* {shownDishList.map((dish, key) => (
            <AdminDishTableRow
            dish={dish}
            key={key}
            selectedHandler={selectItemHandler}
            index={key}
            selectedList={selectedList}
            />
        ))} */}
        </>
    );
    }
