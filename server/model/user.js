class User {
    constructor(id, role, email) {
            this.id = id;
            this.role = this.role;
    }

    getEmail () {
        // TODO: Add email for the user
        return "test@test.com"
    }
}

export default User;